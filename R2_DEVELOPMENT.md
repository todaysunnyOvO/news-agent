# News Agent R2：开发设计与进度稿

> 文档状态：Complete v1.0
> 目标版本：R2  
> 创建日期：2026-09-18  
> 当前状态：R2 Phase 1～4 已完成，等待最终验收确认
> 前置版本：第一轮 MVP（Phase 1～6）已完成  
> 设计原则：延续现有 React、Fastify、SQLite、Repository、Provider、Tool 与 NewsAgentService 边界，不重构现有总体架构。

## 1. R2 定位

R2 将 News Agent 从“可以生成新闻简报的应用”升级为“每天主动服务、能够根据反馈持续改善的个人 AI 新闻助手”。

核心使用闭环：

```text
准时生成 → 主动送达 → 快速阅读 → 轻量反馈 → 次日改善
```

R2 聚焦两个方向：

1. **简报质量闭环**：采集用户对简报和新闻条目的反馈，形成可解释、可撤销的偏好画像，并用于后续选题与排序。
2. **真正的每日助手体验**：按用户习惯稳定生成和主动推送简报，支持失败重试、补发、暂停和清晰的送达状态。

## 2. 产品目标

### 2.1 核心目标

- 每天提供少而精、具有信息增量的 AI 新闻简报；
- 用户无需主动打开网页，也能按计划收到简报；
- 用户可以用极低成本表达“有用”“不感兴趣”“已经知道”等反馈；
- 系统能区分主题偏好、信息新鲜度和重复内容问题；
- 后续简报能够使用反馈改善选题，同时保持过程透明、可控；
- 生成失败和推送失败能够独立处理，不因推送问题重复生成简报；
- 通过固定指标判断 Prompt、模型或排序规则的修改是否真正提升质量。

### 2.2 目标用户体验

用户每天只需 5～10 分钟完成以下操作：

1. 在设定时间收到简报通知；
2. 先阅读三条“今日重点”；
3. 按需查看其他新闻及来源；
4. 对少数条目进行反馈或收藏；
5. 第二天获得更符合兴趣且重复更少的简报。

当当天没有足够重要的新内容时，系统允许生成少于设置上限的条目，不为满足数量而填充低价值信息。

### 2.3 建议质量目标

以下指标作为 R2 初始目标，实际阈值在积累真实使用数据后调整：

| 指标 | 目标 |
|---|---:|
| 单份简报阅读量 | 3～8 条 |
| 用户标记“有用”的比例 | ≥ 70% |
| 用户标记“重复”的比例 | < 10% |
| 调度任务按时启动率 | ≥ 99% |
| 简报成功生成率 | ≥ 95% |
| 成功简报的最终送达率 | ≥ 99% |
| 同一简报重复推送次数 | 0 |

“有用率”只作为综合质量指标之一，不单独驱动标题或内容优化，避免系统演变为点击率导向。

## 3. 非目标

R2 暂不包含：

- 完整账号密码、OAuth 或多租户权限系统；
- Redis、消息队列或分布式任务调度；
- 多实例部署下的选主与分布式锁；
- 向量数据库和复杂推荐系统；
- 无限制的 Agent 长期记忆；
- 同时接入大量新新闻 Provider；
- 自动发布内容或执行投资操作；
- 对现有架构进行大规模重构。

## 4. 设计原则

### 4.1 显式偏好优先

用户在订阅设置中明确填写的偏好始终具有最高优先级。系统从行为反馈中得到的结论只能作为“推断偏好”，不得静默覆盖用户设置。

### 4.2 反馈语义分离

不同反馈代表不同问题：

| 反馈 | 含义 | 建议影响 |
|---|---|---|
| 有用 | 主题、内容深度或来源符合需求 | 提高相关特征权重 |
| 不感兴趣 | 主题或内容类型不符合需求 | 降低相关特征权重 |
| 已经知道 | 内容可能重要，但对用户缺乏新鲜度 | 调整新鲜度和重复策略，不直接降低主题兴趣 |
| 重复/无新进展 | 事件识别或跨日去重不足 | 加强事件级重复抑制 |
| 收藏 | 具有长期参考价值 | 提高相似深度内容权重 |
| 继续追踪 | 用户希望获得后续变化 | 建立显式追踪项 |

### 4.3 生成与送达分离

简报生成和消息推送是两个独立生命周期：

```text
Agent Run ──成功──> Brief ──创建投递──> Delivery Attempt
     │                                  │
     └─失败：重试生成                    └─失败：仅重试投递
```

推送失败不得重新运行 Agent；已经成功送达的简报不得因任务重启而重复发送。

### 4.4 个性化可解释、可撤销、可衰减

- 每条推断偏好记录产生依据；
- 用户可接受、调整、删除或暂停推断偏好；
- 未被持续强化的推断随时间衰减；
- 不将完整反馈历史直接放入模型上下文；
- Agent 只接收经过汇总的短小偏好画像。

### 4.5 保留重大新闻覆盖

个性化不能完全形成信息茧房。对具有明显行业影响的重大事件，即使与近期行为偏好不完全一致，也可以进入“今日重点”，但必须说明推荐原因。

## 5. R2 功能范围

### 5.1 简报条目反馈

每条新闻支持：

- 有用；
- 不感兴趣；
- 已经知道；
- 重复或没有新进展；
- 收藏/取消收藏；
- 继续追踪/停止追踪。

约束：

- 同一用户对同一条目的同类反馈保持幂等；
- 用户可以修改或撤回反馈；
- 保存反馈时保留简报、条目和来源上下文；
- 删除简报时，反馈保留策略需在实现前明确。

### 5.2 整份简报反馈

整份简报支持：

- 有用、一般、没用；
- 内容太少、合适、太多；
- 可选的简短备注；
- “遗漏重要新闻”标记。

自由文本备注只用于用户回顾和后续分析，不直接作为可执行指令传给 Agent。

### 5.3 推断偏好画像

系统定期或在简报生成前汇总近期反馈，形成结构化偏好：

```ts
interface InferredPreference {
  id: string;
  userId: string;
  kind: "topic" | "keyword" | "source" | "content_type" | "freshness";
  value: string;
  weight: number;
  confidence: number;
  status: "suggested" | "accepted" | "dismissed";
  evidenceCount: number;
  lastReinforcedAt: string;
  expiresAt?: string;
}
```

R2 首版使用规则聚合与权重计算，不依赖向量数据库。模型可以帮助归纳展示文案，但权重更新和状态转换由应用层控制。

### 5.4 候选新闻评分

候选文章或事件的建议评分由应用层计算：

```text
基础相关度
+ 显式主题与关键词匹配
+ 已接受的推断偏好
+ 来源质量
+ 信息新鲜度
+ 交叉验证程度
+ 继续追踪加权
- 排除关键词
- 近期重复度
- 负反馈权重
= 候选分数
```

Agent 可以在候选集中进行研究和最终编排，但不应绕过排除词、安全规则和强制去重规则。

### 5.5 简报展示升级

R2 建议输出以下结构：

1. **今日重点**：最多三条最值得立即了解的新闻；
2. **其他值得关注**：与用户偏好匹配的新增内容；
3. **持续追踪**：用户明确追踪事件的实质进展；
4. **今日过滤**：简要说明被过滤的重复、旧闻或低可信内容；
5. **助手观察**：对当天趋势的谨慎总结。

单条新闻建议展示：

- 发生了什么；
- 为什么值得关注；
- 相比已知信息有什么增量；
- 来源及可信状态；
- 原始链接；
- 反馈、收藏和追踪入口。

首轮实现可先保留现有简报数据结构，通过新增可选字段逐步兼容，避免一次性修改所有历史数据。

### 5.6 每日调度体验

设置页提供用户可理解的调度选项：

- 每天；
- 工作日；
- 每周指定日期；
- 本地推送时间；
- IANA 时区；
- 暂停到指定日期；
- 立即恢复；
- 今天不推送。

内部仍可使用 Cron 或现有调度表达式，但 UI 不要求用户理解 Cron。

### 5.7 主动推送

R2 推荐将邮件作为首个面向用户的主要推送渠道，保留现有 HTTPS Webhook。

邮件内容建议包含：

- 简报标题和日期；
- 今日重点三条；
- 每条简短摘要及来源；
- 打开完整简报的链接；
- 带签名的快捷反馈链接；
- 订阅暂停和设置入口。

邮件服务商与具体 API 尚未选型。选型会影响环境变量、API 配置和部署说明，实施前需单独评审。

### 5.8 失败重试与补发

- 生成失败按照可重试错误类型执行有限次数重试；
- 推送失败独立重试，不重新生成简报；
- 使用稳定幂等键避免重复生成和重复发送；
- 支持对已成功生成但未送达的简报手动补发；
- 支持查看每次投递尝试及脱敏错误；
- 错过计划时间时，在允许的补偿窗口内补生成；
- 超过补偿窗口后记录为错过，不发送过时简报。

## 6. 技术设计

### 6.1 架构延续

R2 在现有边界内增加能力：

```text
React Web
├─ 简报阅读与反馈
├─ 收藏与追踪
├─ 推断偏好管理
└─ 调度与投递状态
         │
         ▼
Fastify API
├─ Feedback Routes
├─ Preference Profile Routes
├─ Delivery Routes
└─ Existing User / Run / Brief Routes
         │
    ┌────┴──────────────┐
    ▼                   ▼
NewsAgentService    Delivery Service
    │                   │
    ▼                   ▼
News Tools          Email / Webhook Adapter
    │                   │
    └────────┬──────────┘
             ▼
           SQLite
```

R2 不允许模型直接调用邮件服务。投递由应用层在简报保存成功后创建任务并执行。

### 6.2 建议新增模块

```text
apps/api/src/routes/
├─ feedback.ts
├─ preferences-profile.ts
└─ deliveries.ts

packages/
├─ personalization/src/
│  ├─ feedback-aggregator.ts
│  ├─ preference-profile.ts
│  ├─ candidate-scorer.ts
│  └─ decay.ts
├─ delivery/src/
│  ├─ delivery-service.ts
│  ├─ adapters/email.ts
│  ├─ adapters/webhook.ts
│  ├─ retry-policy.ts
│  └─ signed-actions.ts
└─ db/src/repositories/
   ├─ feedback-repository.ts
   ├─ inferred-preference-repository.ts
   └─ delivery-repository.ts
```

具体目录可在实施前根据当前 workspace 依赖关系微调，但不得破坏现有模块职责。

### 6.3 数据模型草案

#### `brief_feedback`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text PK | 反馈 ID |
| user_id | text FK | 用户 ID |
| brief_id | text FK | 简报 ID |
| usefulness | text nullable | useful/neutral/not_useful |
| length_rating | text nullable | too_short/about_right/too_long |
| missed_important_news | integer | 是否遗漏重要新闻 |
| comment | text nullable | 用户备注，限制长度 |
| created_at | text | 创建时间 |
| updated_at | text | 更新时间 |

约束：`user_id + brief_id` 唯一。

#### `brief_item_feedback`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text PK | 反馈 ID |
| user_id | text FK | 用户 ID |
| brief_item_id | text FK | 简报条目 ID |
| feedback_type | text | useful/not_interested/already_known/repetitive |
| active | integer | 是否仍生效 |
| created_at | text | 创建时间 |
| updated_at | text | 更新时间 |

约束：`user_id + brief_item_id + feedback_type` 唯一。

#### `saved_items`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text PK | 收藏 ID |
| user_id | text FK | 用户 ID |
| brief_item_id | text FK | 简报条目 ID |
| created_at | text | 收藏时间 |

#### `tracked_topics`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text PK | 追踪项 ID |
| user_id | text FK | 用户 ID |
| label | text | 展示名称 |
| query_json | text | 结构化追踪条件 |
| status | text | active/paused/closed |
| source_brief_item_id | text nullable | 创建来源 |
| created_at | text | 创建时间 |
| updated_at | text | 更新时间 |

#### `inferred_preferences`

字段采用 5.3 节接口，并为 `user_id + kind + value` 建立唯一约束。`evidence_json` 仅保存必要的反馈 ID 或统计信息，不复制完整新闻正文。

#### `delivery_jobs`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text PK | 投递任务 ID |
| brief_id | text FK | 简报 ID |
| user_id | text FK | 用户 ID |
| channel | text | email/webhook |
| destination_hash | text | 接收目标的不可逆摘要，用于审计 |
| idempotency_key | text unique | 投递幂等键 |
| status | text | pending/sending/succeeded/failed/cancelled |
| attempt_count | integer | 已尝试次数 |
| next_attempt_at | text nullable | 下次重试时间 |
| delivered_at | text nullable | 成功时间 |
| last_error | text nullable | 脱敏错误 |
| created_at | text | 创建时间 |
| updated_at | text | 更新时间 |

#### `delivery_attempts`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | text PK | 尝试 ID |
| delivery_job_id | text FK | 投递任务 ID |
| attempt_number | integer | 尝试序号 |
| status | text | succeeded/failed |
| provider_message_id | text nullable | 服务商消息 ID |
| error_code | text nullable | 脱敏错误代码 |
| duration_ms | integer | 请求耗时 |
| created_at | text | 尝试时间 |

### 6.4 API 草案

#### 简报反馈

```text
PUT    /api/briefs/:briefId/feedback
GET    /api/briefs/:briefId/feedback
PUT    /api/brief-items/:itemId/feedback/:type
DELETE /api/brief-items/:itemId/feedback/:type
```

#### 收藏与追踪

```text
PUT    /api/brief-items/:itemId/saved
DELETE /api/brief-items/:itemId/saved
GET    /api/users/:userId/saved-items
POST   /api/brief-items/:itemId/tracking
GET    /api/users/:userId/tracked-topics
PATCH  /api/tracked-topics/:trackingId
```

#### 推断偏好

```text
GET    /api/users/:userId/preference-profile
PATCH  /api/users/:userId/personalization
PATCH  /api/inferred-preferences/:preferenceId
DELETE /api/inferred-preferences/:preferenceId
```

#### 投递

```text
GET    /api/briefs/:briefId/deliveries
POST   /api/deliveries/:deliveryId/retry
POST   /api/briefs/:briefId/deliver
```

#### 质量运营

```text
GET    /api/users/:userId/metrics
GET    /api/users/:userId/version-comparison
```

外部邮件中的快捷反馈链接不直接暴露普通写接口。建议使用短时效、单用途签名令牌，并通过独立入口验证后执行受限操作。

### 6.5 Agent 与工具调整草案

建议扩展 `get_user_preferences` 的返回值，加入：

- 已接受的推断偏好摘要；
- 近期强负反馈摘要；
- 明确追踪项；
- 简报最大条目数，而非强制条目数。

建议新增只读领域工具：

```text
get_preference_profile
get_tracked_topics
```

是否将其合并进 `get_user_preferences`，在实施时根据 Prompt 长度和职责清晰度决定。无论采用哪种形式，模型都不获得直接修改偏好权重的工具。

`save_brief` 后续可增加可选字段：

```ts
interface R2BriefItemFields {
  section?: "top" | "more" | "tracking";
  novelty?: "new" | "update" | "ongoing";
  recommendationReason?: string;
  evidenceStatus?: "official" | "corroborated" | "single_source" | "unverified";
}
```

新增字段必须兼容 R1 历史简报。数据库迁移与 API 合同变更实施前需单独说明。

### 6.6 调度与重试策略

建议继续使用当前 Scheduler，并增加状态判断：

1. 扫描到期订阅；
2. 判断暂停、跳过日期和补偿窗口；
3. 使用“用户 + 本地日期 + 简报类型”创建幂等运行；
4. 成功保存简报后创建投递任务；
5. Delivery Service 扫描到期投递任务；
6. 按错误类型决定成功、延迟重试或最终失败。

初始重试建议：

| 场景 | 最大次数 | 建议间隔 |
|---|---:|---|
| Agent 临时网络/服务错误 | 2 | 1 分钟、5 分钟 |
| 邮件或 Webhook 临时失败 | 3 | 1 分钟、5 分钟、30 分钟 |
| 参数、认证或永久错误 | 0 | 直接失败并提示配置问题 |

最终数值应通过测试和真实服务限制确定。

## 7. 安全与隐私

- 邮件地址等投递目标需要加密或按最小权限保存，日志只记录摘要；
- API Key 和邮件服务凭证继续通过环境变量管理；
- 快捷反馈令牌包含用户、目标、动作和过期时间，并使用服务端密钥签名；
- 令牌只能执行预定义反馈动作，不能读取用户其他数据；
- 自由文本反馈限制长度并在页面展示时转义；
- 投递错误信息必须脱敏，不保存认证头和完整响应正文；
- 不向 Agent 暴露邮件地址、签名密钥或投递凭证；
- 用户可以删除反馈、收藏和推断偏好；
- 任何外部邮件服务接入都需要先评审 API 配置、数据发送范围和开发文档变更。

## 8. 可观测性与质量评估

### 8.1 质量指标

- 简报有用率；
- 条目有用率；
- 不感兴趣率；
- 已经知道率；
- 重复率；
- 收藏率；
- 继续追踪率；
- 每份简报实际条目数；
- 来源覆盖和交叉验证率。

### 8.2 送达指标

- 计划任务按时启动率；
- 简报生成成功率；
- 从计划时间到生成完成的延迟；
- 投递成功率；
- 首次投递成功率；
- 平均投递尝试次数；
- 重复投递数；
- 错过简报数。

### 8.3 成本与性能

- 每份简报运行时长；
- Agent Turn 和工具调用数；
- 输入、输出 Token；
- 模型成本；
- 搜索和正文抓取成功率；
- 每条最终入选新闻的平均成本。

### 8.4 评测方式

- 使用固定历史新闻数据集离线回放；
- 为不同反馈画像准备稳定测试案例；
- 对排序结果、排除规则和重大新闻保底进行断言；
- 保存 Prompt、模型和评分规则版本；
- 修改排序、Prompt 或模型前后生成对比报告；
- 不以单次主观观感替代回归测试。

## 9. 测试方案

### 9.1 单元测试

- 各类反馈的创建、修改、撤销和幂等；
- 推断偏好权重更新与时间衰减；
- 显式偏好优先于推断偏好；
- “已经知道”不会错误降低主题兴趣；
- 候选评分和强制排除规则；
- 投递幂等键；
- 重试错误分类和退避时间；
- 快捷反馈签名、过期和篡改检测；
- 暂停、跳过日期和补偿窗口计算。

### 9.2 集成测试

- 反馈能够影响下一次简报候选排序；
- Agent 能读取短小的偏好画像，但不能修改画像；
- 简报保存成功后只创建一次投递任务；
- 投递失败不会重新生成简报；
- 投递重试成功后不会再次发送；
- 邮件 Adapter 使用假服务或本地捕获器，不依赖真实外部服务；
- R1 历史简报仍能通过 API 和前端正常读取。

### 9.3 端到端测试

1. 创建或加载个人用户；
2. 配置每日推送；
3. 生成并查看简报；
4. 对条目反馈、收藏和追踪；
5. 生成下一份简报并验证排序变化；
6. 模拟首次推送失败并自动重试；
7. 验证只收到一份简报；
8. 暂停订阅并验证计划任务不执行；
9. 恢复后验证下一计划周期正常运行。

## 10. R2 验收标准

- [x] 用户可以对单条新闻提交、修改和撤销反馈；
- [x] 用户可以评价整份简报；
- [x] 用户可以收藏新闻并查看收藏列表；
- [x] 用户可以创建、暂停和停止追踪项；
- [x] 系统能生成可查看、可撤销的推断偏好；
- [x] 后续简报排序会使用反馈汇总，而不是直接拼接历史反馈；
- [x] 用户设置的显式偏好不会被系统推断覆盖；
- [x] 简报允许少于最大条目数，不强制凑数；
- [x] 用户可以配置易理解的每日或工作日计划；
- [x] 成功生成的简报可以通过至少一个主动渠道送达；
- [x] 生成状态与投递状态独立保存；
- [x] 临时投递失败可以自动重试；
- [x] 手动补发不会重新生成简报；
- [x] 已成功投递的简报不会重复发送；
- [x] 邮件快捷反馈链接具备签名、时效和动作限制；
- [x] R1 历史数据和现有 API 保持兼容；
- [x] 全仓库 Lint、TypeScript 和测试通过；
- [x] 固定评测集可输出质量、成本与性能对比。

## 11. 分阶段开发计划

### R2 Phase 1：反馈基础

- 定义反馈枚举和共享合同；
- 新增简报与条目反馈表；
- 新增收藏和追踪数据模型；
- 实现 Repository 和 API；
- 在简报页面增加反馈控件；
- 完成反馈幂等和权限测试。

交付结果：用户能够表达对简报和条目的真实偏好，数据结构可用于后续个性化。

### R2 Phase 2：每日送达最小闭环

- 明确邮件服务选型；
- 建立 Delivery Service 和 Adapter 接口；
- 将现有 Webhook 纳入统一投递生命周期；
- 实现邮件模板和测试捕获器；
- 实现投递幂等、重试与补发；
- 增加调度暂停、工作日和状态展示。

交付结果：简报能够按计划主动送达，失败可恢复且不会重复发送。

### R2 Phase 3：个性化选题

- 实现反馈聚合器；
- 生成可解释的推断偏好；
- 实现权重衰减；
- 实现候选评分器；
- 将偏好画像安全传给 Agent；
- 增加查看、接受、删除和暂停推断的 UI；
- 验证重大新闻保底策略。

交付结果：后续简报可以根据反馈改善，同时用户能够理解并控制个性化。

### R2 Phase 4：质量运营与收口

- 升级简报展示结构；
- 增加质量和送达指标；
- 建立固定历史数据回放集；
- 输出版本对比报告；
- 完成兼容性、安全和端到端测试；
- 更新 README、DEVELOPMENT 和 API 配置说明；
- 完成 R2 验收。

交付结果：质量变化可衡量，R2 可以作为稳定的个人每日助手使用。

## 12. 开发进度表

### 12.1 状态说明

| 标记 | 状态 | 含义 |
|---|---|---|
| ⬜ | 未开始 | 尚未进入开发 |
| 🟦 | 进行中 | 已开始但未完成 |
| 🟨 | 待验证 | 已实现，等待测试或评审 |
| ✅ | 已完成 | 已通过验收 |
| ⛔ | 阻塞 | 存在外部依赖或待决策问题 |

更新规则：

- 开始任务时更新为“进行中”；
- 代码完成但未通过对应测试时更新为“待验证”；
- 只有满足产物或验收条件后更新为“已完成”；
- 阻塞项必须记录原因、负责人和下一步；
- 每个阶段完成时同步更新总体进度和最后更新日期。

### 12.2 总体进度

| 阶段 | 状态 | 进度 | 目标产物 | 实际完成日期 |
|---|---|---:|---|---|
| R2 Phase 1：反馈基础 | ✅ 已完成 | 100% | 条目反馈、简报评价、收藏和追踪 | 2026-09-18 |
| R2 Phase 2：每日送达最小闭环 | ✅ 已完成 | 100% | 邮件/Webhook 统一投递、重试和补发 | 2026-09-18 |
| R2 Phase 3：个性化选题 | ✅ 已完成 | 100% | 推断偏好、候选评分与用户控制 | 2026-09-18 |
| R2 Phase 4：质量运营与收口 | ✅ 已完成 | 100% | 新简报体验、评测与完整验收 | 2026-09-18 |

总体状态：✅ R2 Phase 1～4 已完成，等待最终验收确认

总体进度：100%
最后更新日期：2026-09-18

### 12.3 任务明细

| ID | 阶段 | 任务 | 状态 | 依赖 | 产物或验收条件 | 备注 |
|---|---|---|---|---|---|---|
| R2-P1-01 | Phase 1 | 定义反馈与收藏共享合同 | ✅ 已完成 | R1 | 类型、枚举和校验规则明确 | 保持 API 兼容 |
| R2-P1-02 | Phase 1 | 新增反馈、收藏和追踪数据库迁移 | ✅ 已完成 | R2-P1-01 | 迁移可重复执行，旧数据可读取 | 新增 0004 迁移，幂等迁移测试通过 |
| R2-P1-03 | Phase 1 | 实现反馈 Repository | ✅ 已完成 | R2-P1-02 | CRUD、幂等和撤销测试通过 | 包含用户所有权校验 |
| R2-P1-04 | Phase 1 | 实现收藏与追踪 Repository | ✅ 已完成 | R2-P1-02 | 收藏和追踪状态测试通过 | 收藏、暂停、恢复和停止均可用 |
| R2-P1-05 | Phase 1 | 实现反馈、收藏和追踪 API | ✅ 已完成 | R2-P1-03～04 | API 集成测试通过 | 非所属资源统一拒绝 |
| R2-P1-06 | Phase 1 | 实现简报页反馈控件 | ✅ 已完成 | R2-P1-05 | 可提交、撤回并显示当前状态 | 条目和整份简报反馈均已实现 |
| R2-P1-07 | Phase 1 | 实现收藏和追踪页面 | ✅ 已完成 | R2-P1-05 | 可查看、暂停和取消 | 新增收藏与追踪视图 |
| R2-P1-08 | Phase 1 | 完成反馈端到端测试 | ✅ 已完成 | R2-P1-06～07 | 主流程和异常流程通过 | 全仓库 10 个测试文件、41 项测试通过 |
| R2-P2-01 | Phase 2 | 评审并选择邮件服务 | ✅ 已完成 | 无 | 明确 API、费用、配置和数据范围 | 已确认 Resend，发件域名 mail.dailynewsagent.site |
| R2-P2-02 | Phase 2 | 设计统一 Delivery Adapter | ✅ 已完成 | R2-P2-01 | 邮件和 Webhook 接口一致 | Agent 不接触投递凭证 |
| R2-P2-03 | Phase 2 | 新增投递任务和尝试记录迁移 | ✅ 已完成 | R2-P2-02 | 状态、幂等和审计字段完整 | 新增 0005 迁移并保留 R1 投递记录 |
| R2-P2-04 | Phase 2 | 实现 Delivery Service | ✅ 已完成 | R2-P2-03 | 创建、发送、重试和终止逻辑通过 | 数据库持久化重试，无阻塞等待 |
| R2-P2-05 | Phase 2 | 迁移现有 Webhook 推送 | ✅ 已完成 | R2-P2-04 | 现有功能保持兼容 | Webhook 已纳入统一状态机 |
| R2-P2-06 | Phase 2 | 实现邮件 Adapter 与模板 | ✅ 已完成 | R2-P2-01、04 | 测试环境可捕获邮件 | Resend 离线测试及真实 163 邮箱投递均通过 |
| R2-P2-07 | Phase 2 | 实现带签名的快捷反馈 | ✅ 已完成 | R2-P1-05、R2-P2-06 | 篡改和过期测试通过 | 7 天有效、限定反馈动作 |
| R2-P2-08 | Phase 2 | 增加暂停、工作日和补偿调度 | ✅ 已完成 | R1 Scheduler | 时区和错过执行测试通过 | 支持每日、工作日、暂停、跳过今天和 4 小时补偿 |
| R2-P2-09 | Phase 2 | 实现投递状态和手动补发 UI | ✅ 已完成 | R2-P2-04 | 可诊断失败并安全补发 | 展示脱敏错误和每次尝试 |
| R2-P2-10 | Phase 2 | 完成送达端到端测试 | ✅ 已完成 | R2-P2-05～09 | 失败重试且不重复发送 | 离线 E2E 与真实 Resend 单次投递通过 |
| R2-P3-01 | Phase 3 | 实现反馈聚合器 | ✅ 已完成 | Phase 1 | 不同反馈语义正确分离 | 有用、不感兴趣、已知和重复信号分开处理 |
| R2-P3-02 | Phase 3 | 实现推断偏好数据模型 | ✅ 已完成 | R2-P3-01 | 来源、置信度和状态可追溯 | 新增 0006 迁移，记录证据、置信度、状态和过期时间 |
| R2-P3-03 | Phase 3 | 实现权重更新与衰减 | ✅ 已完成 | R2-P3-02 | 固定时间测试通过 | 30 天半衰期、90 天过期，用户接受后的权重可调 |
| R2-P3-04 | Phase 3 | 实现候选新闻评分器 | ✅ 已完成 | R2-P3-02～03 | 相同输入结果稳定可解释 | 输出个性化分数与推荐原因，显式排除词为硬约束 |
| R2-P3-05 | Phase 3 | 向 Agent 提供精简偏好画像 | ✅ 已完成 | R2-P3-04 | 不传完整反馈历史 | 复用 get_user_preferences，仅传接受偏好、近期负向摘要和追踪主题 |
| R2-P3-06 | Phase 3 | 实现推断偏好管理 API 与 UI | ✅ 已完成 | R2-P3-02 | 可接受、删除和暂停 | 支持接受、忽略、调权、删除及全局暂停，含所有权校验 |
| R2-P3-07 | Phase 3 | 实现重大新闻保底策略 | ✅ 已完成 | R2-P3-04 | 回归数据集验证通过 | 权威来源高影响事件设置分数下限，但不绕过排除词 |
| R2-P3-08 | Phase 3 | 完成个性化回放测试 | ✅ 已完成 | R2-P3-04～07 | 不同画像产生合理排序差异 | 固定时钟回放、画像差异、重大新闻和 API 测试通过；全仓库 53 项测试通过 |
| R2-P4-01 | Phase 4 | 升级简报结构与兼容渲染 | ✅ 已完成 | Phase 3 | R1/R2 简报均可展示 | 0007 可空字段迁移；栏目、进展、推荐原因和证据状态兼容显示 |
| R2-P4-02 | Phase 4 | 增加质量指标聚合 | ✅ 已完成 | Phase 1 | 指标可按时间查询 | 聚合有用率、负向反馈、收藏追踪和独立来源覆盖 |
| R2-P4-03 | Phase 4 | 增加送达与可靠性指标 | ✅ 已完成 | Phase 2 | 可定位生成和投递失败 | 聚合生成/投递成功率、首次成功、尝试次数、重复和遗漏 |
| R2-P4-04 | Phase 4 | 建立固定历史新闻评测集 | ✅ 已完成 | R1 Mock Provider | 测试数据稳定、无外网依赖 | 固定时间与候选数据覆盖画像、重大新闻和排除规则 |
| R2-P4-05 | Phase 4 | 实现版本对比报告 | ✅ 已完成 | R2-P4-02～04 | 输出质量、成本和性能差异 | 提供相邻周期对比、版本标记和离线回放得分 |
| R2-P4-06 | Phase 4 | 完成安全与兼容性检查 | ✅ 已完成 | Phase 1～3 | R1 数据、API 和安全测试通过 | R1/R2 同 API 读取、聚合报告脱敏；生产依赖 npm audit 为 0 |
| R2-P4-07 | Phase 4 | 更新项目文档与 API 配置说明 | ✅ 已完成 | R2-P4-06 | README、开发文档和示例配置同步 | README、DEVELOPMENT 与本文已同步；无新增环境变量 |
| R2-P4-08 | Phase 4 | 完成 R2 验收 | ✅ 已完成 | R2-P4-01～07 | 第 10 节全部适用项通过 | 第 10 节全部通过；全仓库 14 个测试文件、55 项测试通过 |

## 13. 待决策事项

以下事项需要在进入对应开发阶段前确定：

| 决策 | 推荐方案 | 决策时点 |
|---|---|---|
| 第一主动推送渠道 | 邮件，现有 Webhook 保留 | 已确认 |
| 邮件服务商 | Resend；发件域名 `mail.dailynewsagent.site`，个人 163 邮箱由环境变量配置 | 已确认 |
| 推断偏好默认状态 | suggested，用户可接受或删除 | Phase 3 开始前 |
| 反馈影响窗口 | 近期反馈高权重，随时间衰减 | R2-P3-03 |
| 简报默认最大条目数 | 保留当前设置，上限而非目标值 | Phase 3 开始前 |
| 补偿生成窗口 | 建议计划时间后 4 小时内 | R2-P2-08 |
| 快捷反馈令牌时效 | 建议 7 天，动作受限 | R2-P2-07 |

## 14. 主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 反馈数据早期过少 | 个性化结论不稳定 | 设置最小证据数和置信度，不急于自动生效 |
| 单次负反馈影响过大 | 错误屏蔽重要主题 | 权重上限、衰减和多次证据要求 |
| 个性化形成信息茧房 | 遗漏重大新闻 | 设置重大新闻保底和来源质量约束 |
| 邮件进入垃圾箱 | 实际送达率下降 | 选择可靠服务商并配置域名认证和退信处理 |
| 投递重试导致重复邮件 | 体验受损 | 幂等键、状态机和服务商消息 ID |
| 快捷反馈链接被滥用 | 反馈数据污染 | 短时签名、限定动作、可撤销和审计 |
| 新字段破坏历史简报 | R1 数据不可用 | 可选字段、兼容渲染和迁移回归测试 |
| Prompt 体积随反馈增长 | 成本和效果恶化 | 只传聚合画像，限制条目与字符数 |
| 指标驱动标题党 | 内容质量下降 | 综合质量指标，不以点击率作为唯一目标 |

## 15. 建议启动顺序

R2 建议从 Phase 1 开始，但在写数据库迁移前先完成一次 Schema 与 API 评审。推荐首个可交付切片：

1. 为单条新闻增加“有用、不感兴趣、已经知道、重复”反馈；
2. 支持撤销，并保证接口幂等；
3. 在简报详情页显示反馈状态；
4. 增加反馈 Repository、API 和端到端测试；
5. 暂不让反馈自动影响 Agent，先积累可验证数据。

完成该切片后，再进入收藏、追踪和每日送达，能够以较低风险尽早形成真实使用闭环。
