# 根目录 `.env` 未加载问题

## 现象

配置了 DeepSeek 和 Tavily Key，但生成结果仍是本地 Mock 简报。

## 原因

通过 npm workspace 启动 API 时，工作目录是 `apps/api`。原代码按相对路径读取 `.env`，因此查找的是 `apps/api/.env`，没有读取项目根目录的 `.env`，随后回退到 Demo Runtime。

## 修复

API 启动时根据 `server.ts` 的位置解析项目根目录，并显式加载根目录 `.env`。修复后重新启动 API 即可启用 DeepSeek + Tavily 真实运行时。

---

# “跳过今天”和“立即投递”缺少就地反馈

## 现象

- 点击“今天不推送”后，按钮没有状态变化，成功或失败信息只显示在页面顶部；滚动到设置页底部时看起来像没有生效。
- 新建身份尚未保存订阅时点击该按钮，请求返回 404，但当前位置没有错误提示。
- 推送渠道为“仅站内”时，简报页仍显示“立即投递”；点击后接口返回 409，错误同样只显示在页面顶部。
- 设置页右上角固定显示“站内简报”，即使表单已选择邮件或 Webhook，也容易误判当前渠道。

## 原因

前端把所有操作结果统一写入页面顶部的全局状态栏，操作按钮附近没有局部状态；同时没有在发起请求前根据订阅是否已保存、当天是否已经跳过、推送渠道是否支持外部投递进行判断。

## 修复

- “今天不推送”改为“跳过今天的自动简报”，明确其只影响自动任务。
- 请求期间显示“处理中”，成功后按钮变为“今天已跳过”并禁用，同时显示“明天会按原计划恢复”。
- 未保存订阅时在按钮旁提示先保存，不再发送必然失败的请求。
- “仅站内”模式不再显示可误解的“立即投递”，改为“配置邮件投递”，点击后返回偏好设置。
- 邮件和 Webhook 模式显示明确目标、处理中状态、成功结果及就地错误信息；失败投递的重试也显示进度。
- 设置页渠道徽标随当前选择显示“站内简报”“邮件简报”或“Webhook”。

---

# 未保存的推送渠道被误用于立即投递

## 现象

用户已保存的推送渠道为“仅站内”时，在设置页把渠道改为“邮件”但不保存，简报页会错误显示“立即投递到邮箱”。点击后端点后，服务端读取数据库中仍为 `web` 的订阅并返回 `Select email or webhook delivery first`。

## 原因

前端使用同一个 `form` 状态同时表示正在编辑的订阅草稿和后端已经保存的订阅。投递按钮、渠道文案和前置判断读取了草稿渠道，而后端投递服务始终读取 SQLite 中的已保存渠道，导致前后端对当前生效渠道的判断不一致。原有 `subscriptionSaved` 只能表示订阅是否存在，不能识别表单是否有未保存修改。

## 修复

- 将前端订阅状态拆分为 `draftSubscription` 和 `savedSubscription`；表单只修改草稿，保存成功后才更新已保存快照。
- 简报页的渠道文案、投递按钮和投递前置判断全部改为基于 `savedSubscription`。
- 已保存渠道为站内、草稿改为邮件或 Webhook 时，显示“设置尚未保存”，引导返回设置页，不再发送必然返回 409 的请求。
- 订阅存在其他未保存修改时，投递入口要求先保存，并说明当前实际生效的已保存渠道。
- 设置页徽标和保存按钮附近显示未保存、保存中、成功与失败状态；离开设置页或关闭页面时提示仍有未保存修改。
- 从设置页点击“立即生成”时，如果存在未保存修改则先阻止启动并提示保存，避免实际生成仍使用旧订阅。
- “跳过今天”和个性化开关只合并各自更新的已保存字段，不再用后端响应覆盖整份订阅草稿。
- 增加订阅草稿/已保存快照比较测试，覆盖渠道未保存、无已保存订阅和数组引用隔离。

## 验证结果

- `npm run check` 通过：15 个测试文件、59 项测试全部通过。
- `npm run build` 通过，API 与 Web 生产构建成功。
- `git diff --check` 通过；仅保留 Windows 工作区已有的 LF/CRLF 转换提示。
- 自动化验证确认状态比较与构建链路正常；尚未将测试通过夸大为真实邮件服务或完整人工交互验收。

---

# 日常阅读流程被配置、反馈和投递管理操作淹没

## 现象

- 已有用户进入产品后需要先理解订阅、运行和历史等内部概念，不能直接开始阅读今日简报。
- 一级导航和每条新闻周围同时暴露大量低频操作，首次设置、日常阅读和异常恢复的层级不清晰。
- 正常投递仍突出手动操作，发送结果、失败影响和恢复入口混在技术任务详情中。

## 原因

前端信息架构按已有功能模块组织，而不是按“首次配置、阅读今日简报、轻量反馈、失败恢复”等用户任务组织。高级诊断能力、负反馈原因和投递任务状态没有渐进披露，导致低频能力长期占用主要注意力。

## 修复

- 已有用户默认进入“今日”，一级导航缩减为“今日 / 收藏与追踪 / 设置”，历史和生成状态回到阅读上下文。
- 新用户使用三步首次设置；设置页按内容、时间和接收方式分区，高级来源、Webhook 和诊断能力默认折叠或降级。
- 每条新闻默认只显示有帮助、不喜欢和收藏；负反馈原因按需展开，继续追踪只在收藏后出现。
- 投递区域改为结果优先，只在失败时突出重新发送，并把技术错误映射为不暴露敏感信息的用户文案。
- 异步反馈、收藏、追踪、设置和投递状态尽量显示在原操作附近，并增加禁用和防重复操作。

## 验证结果

- UX R1 跨阶段任务测试覆盖首次设置、今日阅读、渐进反馈和投递失败恢复状态。
- 本地现有数据库中的历史简报可通过当前 API 读取；缺少 R2 扩展字段的旧条目仍可返回详情。
- 全量工程检查、生产构建、差异检查以及 API/Web 本地启动检查均纳入 `UX_DEVELOPMENT.md` 的分阶段记录。
- 这些结果证明代码路径和状态规则通过自动化验证，不代表已经完成真实用户可用性测试。
