# News Agent 体验优化：开发计划与进度表

> 文档状态：Planning v0.1  
> 计划版本：UX R1  
> 创建日期：2026-09-18  
> 当前状态：UX R1 Phase 0～5 实施完成，等待真实使用反馈或下一轮计划  
> 前置版本：MVP Phase 1～6、R2 Phase 1～4 已完成  
> 核心原则：优先做信息架构和交互减法，不改变 React → Fastify API → Repository/Service → SQLite 的总体架构。

## 1. 背景与问题判断

News Agent 已具备订阅设置、Agent 生成、历史简报、邮件/Webhook 投递、反馈、收藏、追踪、个性化画像和质量指标等完整能力，但现有界面把大量系统能力同时暴露给用户。

当前体验更接近“News Agent 管理后台”，而不是“每天打开即可阅读的个人新闻助手”。用户必须理解生成、保存、投递、重试和个性化之间的关系，才能完成本应简单的日常任务。

本轮判断来自现有界面和操作路径的启发式检查，尚未经过正式用户访谈或可用性测试，不能视为真实用户研究结论。

### 1.1 主要体验问题

1. **默认入口偏离核心任务**：产品首先展示身份和订阅配置，而不是今日简报。
2. **一级入口过多**：偏好设置、个性化画像、质量概览、运行详情、历史简报、收藏追踪和立即生成处于相近层级。
3. **内部概念暴露过多**：运行详情、工具调用、投递任务、Webhook 和指标更适合高级或诊断场景。
4. **日常操作链过长**：设置、保存、生成、等待、打开历史、选择简报和投递之间缺少自然串联。
5. **反馈操作密度过高**：每条新闻同时展示多种反馈、收藏和追踪操作，影响阅读节奏。
6. **状态层级不清晰**：设置保存、简报生成和外部投递的状态容易被用户混为一谈。
7. **异常处理主导界面**：重试、补发和运行过程占据过多注意力，而正常阅读路径不够突出。

## 2. 体验目标

### 2.1 核心产品目标

将产品从“配置和管理 Agent”调整为“阅读今日简报并用轻量反馈持续改善”的日常工具。

用户打开产品后应能快速回答三个问题：

1. 今天有没有新简报？
2. 哪几条最值得阅读？
3. 我是否需要反馈、收藏或调整设置？

### 2.2 目标操作路径

日常路径：

```text
打开产品 → 阅读今日简报 → 对少量条目反馈 → 离开
```

首次使用路径：

```text
选择关注内容 → 设置时间与接收方式 → 保存并生成第一份简报
```

异常恢复路径：

```text
看到就地错误 → 理解影响范围 → 重试或前往对应设置
```

### 2.3 初始体验指标

这些指标用于后续验证方向，不在没有真实数据时预先宣称达标：

| 指标 | 初始目标 |
|---|---:|
| 已有简报时，从打开产品到开始阅读 | 不超过 1 次点击 |
| 首次配置主流程 | 不超过 3 个步骤 |
| 一级导航数量 | 不超过 3 个 |
| 单条新闻默认可见反馈操作 | 不超过 3 个 |
| 正常投递是否需要用户手动操作 | 否 |
| 异步操作是否具备就地状态 | 100% |
| 必然失败的请求 | 前端阻止，同时保留后端校验 |

## 3. 设计原则

### 3.1 今日优先

登录或恢复本地身份后默认进入“今日”，而不是设置页。最新简报是页面主内容，配置和诊断能力退居次级入口。

### 3.2 渐进披露

普通用户默认只看到完成当前任务所需的信息。高级设置、运行详情、质量指标和 Webhook 在用户主动展开时出现。

### 3.3 正常路径优先

正常情况下系统按计划自动生成和投递。重试、补发和详细运行日志只在失败或用户主动查看时出现。

### 3.4 状态靠近操作

保存、生成、投递、重试和反馈的处理中、成功、失败和禁用原因都显示在操作位置附近。页面顶部状态仅承担全局通知，不替代局部反馈。

### 3.5 使用用户语言

界面优先使用“今日简报”“接收方式”“重新发送”等用户语言，减少“Delivery Job”“Webhook”“Agent Run”等内部实现术语。

### 3.6 保留高级能力但降低干扰

本轮不删除已有能力。运行详情、质量概览、画像管理和 Webhook 将移动到高级入口或上下文入口，仍可访问和诊断。

## 4. 目标信息架构

一级导航缩减为三个入口：

```text
今日
├─ 今日简报
├─ 生成状态或失败恢复
└─ 往期简报入口

收藏与追踪
├─ 收藏内容
└─ 持续追踪

设置
├─ 内容偏好
├─ 时间安排
├─ 接收方式
├─ 个性化
└─ 高级设置
   ├─ Webhook
   ├─ 运行详情
   └─ 质量概览
```

### 4.1 “今日”页面

根据状态显示不同内容：

- **已有今日简报**：直接展示标题、更新时间、投递状态和新闻条目。
- **正在生成**：显示单一进度卡片，详细工具时间线按需展开。
- **今日尚未生成**：显示“生成今天的简报”主按钮。
- **生成失败**：在卡片内说明影响和可执行的恢复操作。
- **今日已跳过**：说明自动任务已跳过，但仍允许手动生成。

历史简报通过日期切换或“往期简报”进入，不再占用一级导航。

### 4.2 “收藏与追踪”页面

保留当前收藏和追踪能力，统一空状态、暂停、恢复和停止操作的反馈。

### 4.3 “设置”页面

按用户任务拆分为内容偏好、时间安排和接收方式。个性化画像改为可解释的“为你推荐”，复杂权重调整放入高级区域。

邮件作为普通用户的主要外部接收方式；Webhook 放入高级设置。

## 5. 关键交互方案

### 5.1 首次使用

首次使用采用三步引导：

1. 选择关注话题和关键词；
2. 设置生成频率、时间和时区；
3. 选择站内或邮件接收。

最终主操作为“保存并生成第一份简报”。来源限制、排除词、Webhook 和高级个性化不阻塞首次使用。

### 5.2 简报生成

- 从“今日”发起生成后留在同一上下文显示进度。
- 生成完成后自动展示最新简报，不要求用户进入历史列表再次选择。
- 运行详情默认折叠，仅在失败诊断或高级设置中完整展示。
- 存在未保存订阅修改时，明确说明本次会使用哪份设置，并要求先保存或放弃修改。

### 5.3 新闻反馈

每条新闻默认只展示：

- 有帮助；
- 不喜欢；
- 收藏。

点击“不喜欢”后再选择“不感兴趣”“已经知道”或“重复/无新进展”。“继续追踪”作为收藏后的二级操作或更多菜单操作。

### 5.4 投递

- 正常生成后按已保存渠道自动投递。
- 页面主要显示“已发送到邮箱”或“仅保存在站内”等结果状态。
- 仅在失败时突出“重新发送”。
- 已成功投递后的再次发送放入次级操作，避免重复发送成为常规动作。
- 投递判断始终基于后端已保存订阅，不使用未保存表单草稿。

### 5.5 设置保存

第一阶段继续保留显式保存，确保改动范围可控，并明确区分草稿和已保存状态。

后续可评估分区自动保存：普通偏好可延迟自动保存；邮件和 Webhook 等有外部影响的设置继续使用明确确认。启用自动保存前必须评审失败恢复、并发更新和 API 请求频率。

## 6. 分阶段开发计划

### UX Phase 1：今日优先与导航减法

目标：在不改变 API 和数据库的前提下，让日常阅读成为默认体验。

- 新增“今日”视图并设为已有用户的默认入口；
- 自动加载并展示今日或最新简报；
- 将一级导航缩减为“今日”“收藏与追踪”“设置”；
- 将历史列表合并为今日页中的往期入口；
- 将运行详情、质量概览和个性化画像移动到高级入口；
- 生成完成后自动回到今日简报；
- 为无简报、生成中、生成失败和今日已跳过提供明确状态卡片；
- 保留旧能力入口，避免功能丢失。

本阶段预计不涉及 API、数据库、环境变量或总体架构调整。

### UX Phase 2：首次使用与设置分区

目标：降低首次配置成本，并让设置结构更符合用户语言。

- 实现三步首次使用引导；
- 将设置拆分为内容、时间和接收方式；
- 将高级来源、排除词、Webhook 和诊断功能折叠；
- 为邮件配置增加清晰的启用状态和保存结果；
- 统一未保存修改、离开提醒和保存失败恢复；
- 评估是否引入分区自动保存。

如果采用自动保存或修改订阅 API 合同，实施前必须单独说明并确认。

### UX Phase 3：阅读与反馈减法

目标：减少每条新闻周围的操作噪音，让用户首先阅读内容。

- 默认只展示三个反馈操作；
- 将负反馈原因改为二级选择；
- 将继续追踪移动到收藏后动作或更多菜单；
- 优化栏目、来源、推荐原因和可信状态的视觉层级；
- 增加操作中的禁用、防重复、成功和失败反馈；
- 验证键盘操作、触屏尺寸和基础无障碍标签。

本阶段预计复用现有反馈 API，不新增反馈类型。

### UX Phase 4：投递与异常恢复

目标：让正常投递退居后台，让失败恢复清晰可控。

- 正常状态只显示投递结果，不突出手动投递；
- 失败时显示原因、影响范围和重新发送入口；
- 成功后的再次投递降为次级操作；
- 统一生成失败、投递失败和配置缺失的错误文案；
- 确认邮件、Webhook 和站内模式的所有组合状态；
- 增加不重复投递和错误就地展示的回归测试。

本阶段默认不修改投递状态机。若发现当前 API 无法表达必要状态，再单独提出合同调整方案。

### UX Phase 5：验证与收口

目标：用实际任务验证复杂度是否下降，而不是只验证代码可以运行。

- 建立首次配置、日常阅读、反馈和错误恢复自动化场景；
- 运行完整回归、构建和本地启动检查；
- 记录自动化覆盖范围和仍未验证的真实用户体验；
- 验证 R1/R2 历史数据和现有功能仍可访问；
- 运行全量检查、构建和风险匹配的自动化测试；
- 根据实际观察更新本文档和 bug 修复记录。

## 7. 技术边界与预期改动

### 7.1 保持不变

- React → Fastify API → Repository/Service → SQLite 架构；
- Agent 通过受控 Tool 使用新闻能力；
- 简报、反馈、收藏、追踪和投递的现有数据模型；
- 生成与投递分离的生命周期；
- 后端对订阅状态和投递幂等的最终校验。

### 7.2 主要前端改动

- 将当前大型 `App.tsx` 按视图和领域逐步拆分为可测试组件；
- 引入明确的页面状态模型，避免同一变量同时表示草稿、已保存值和服务端运行状态；
- 将导航、今日简报、设置区块和异步状态提取为独立组件；
- 增加纯状态函数测试和必要的组件交互测试。

组件拆分属于现有 React 前端内部整理，不改变总体架构。若拆分范围明显扩大，应按小切片实施，避免与现有未提交功能改动混杂。

### 7.3 需要提前确认的变化

以下情况出现时必须暂停并单独评审：

- 新增或修改 API 合同；
- 新增数据库字段或迁移；
- 引入新的环境变量或外部服务；
- 将设置改为自动保存并改变现有持久化语义；
- 删除现有功能或改变历史数据兼容方式；
- 调整生成或投递状态机。

## 8. 测试与验证计划

### 8.1 自动化测试

- 默认入口根据用户和简报状态选择正确视图；
- 今日简报加载、空状态和失败状态；
- 未保存设置不会被当作已生效配置；
- 生成完成后展示最新简报；
- 精简反馈控件仍调用正确的现有 API；
- 投递成功、失败、重试和幂等状态正确显示；
- R1/R2 历史简报保持兼容。

每个实施切片至少运行：

```text
npm run check
npm run build
git diff --check
```

### 8.2 本地运行检查

每个阶段完成代码和自动化验证后启动项目，并至少确认：

- API 健康检查可访问；
- Web 首页可访问；
- 开发服务器提供的是当前工作区代码；
- 启动日志没有阻塞性错误；
- 不因本地启动而调用真实邮件或 Webhook 投递。

自动化测试和本地启动通过不等于真实用户体验已经验证。阶段报告必须说明自动化覆盖范围，不宣称已经完成真实用户验收。

## 9. UX R1 验收标准

- [x] 已有用户默认进入“今日”，无需先经过设置页；
- [x] 已有今日简报时，打开产品后最多一次点击即可开始阅读；
- [x] 一级导航不超过三个；
- [x] 历史简报、运行详情、质量概览和个性化能力仍可访问；
- [x] 首次配置主流程不超过三个步骤；
- [x] 单条新闻默认反馈操作不超过三个；
- [x] 生成完成后自动展示最新简报；
- [x] 正常投递不要求用户手动点击“立即投递”；
- [ ] 所有异步主操作均有就地的处理中、成功、失败、禁用和防重复状态；核心阅读、设置、反馈和投递流程已覆盖，画像调权及追踪暂停/恢复等低频高级操作仍主要依赖全局提示；
- [x] 未保存草稿不会影响已保存订阅的能力判断；
- [x] API、数据库或配置变化均经过单独确认；
- [x] 全量检查、构建和 diff 检查通过；
- [x] 项目可在本地启动，API 健康检查和 Web 首页可访问；
- [x] 未把自动化测试结果表述为真实用户体验已经验证。

## 10. 开发进度表

### 10.1 状态说明

| 标记 | 状态 | 含义 |
|---|---|---|
| ⬜ | 未开始 | 尚未进入实施 |
| 🟦 | 进行中 | 已开始，但尚未完成代码或体验验证 |
| 🟨 | 待验证 | 已实现，等待完整自动化测试、构建或本地启动检查 |
| ✅ | 已完成 | 代码、自动化检查、构建和本地启动检查均完成 |
| ⛔ | 阻塞 | 需要用户决策、外部条件或架构/API 评审 |

### 10.2 总体进度

| 阶段 | 状态 | 进度 | 目标产物 | 实际完成日期 |
|---|---|---:|---|---|
| UX Phase 0：问题梳理与计划 | ✅ 已完成 | 100% | 体验诊断、目标信息架构和实施计划 | 2026-09-18 |
| UX Phase 1：今日优先与导航减法 | ✅ 已完成 | 100% | 今日首页、三入口导航、上下文状态 | 2026-09-18 |
| UX Phase 2：首次使用与设置分区 | ✅ 已完成 | 100% | 三步引导和分区设置 | 2026-09-19 |
| UX Phase 3：阅读与反馈减法 | ✅ 已完成 | 100% | 精简阅读和渐进反馈 | 2026-09-19 |
| UX Phase 4：投递与异常恢复 | ✅ 已完成 | 100% | 结果优先的投递状态与失败恢复 | 2026-09-19 |
| UX Phase 5：验证与收口 | ✅ 已完成 | 100% | 自动化回归、本地运行检查和文档收口 | 2026-09-19 |

总体状态：✅ UX R1 Phase 0～5 实施完成；真实用户验收和低频高级操作就地反馈仍待后续验证  
总体实施进度：100%  
最后更新日期：2026-09-19

### 10.3 任务明细

| ID | 阶段 | 任务 | 状态 | 依赖 | 产物或验收条件 | 备注 |
|---|---|---|---|---|---|---|
| UX-P0-01 | Phase 0 | 梳理现有操作路径和复杂度来源 | ✅ 已完成 | R2 | 形成问题清单 | 启发式检查，非正式用户研究 |
| UX-P0-02 | Phase 0 | 定义目标信息架构 | ✅ 已完成 | UX-P0-01 | 三个一级入口及功能归属明确 | 等待实施确认 |
| UX-P0-03 | Phase 0 | 编写开发计划和进度表 | ✅ 已完成 | UX-P0-02 | 本文档可用于逐项跟踪 | 2026-09-18 |
| UX-P1-01 | Phase 1 | 定义今日页状态模型 | ✅ 已完成 | UX-P0 | 覆盖无简报、生成中、成功、失败和跳过 | 新增纯状态模型及 5 项测试 |
| UX-P1-02 | Phase 1 | 新增今日页并设为已有用户默认入口 | ✅ 已完成 | UX-P1-01 | 恢复身份后直接看到今日状态 | 新用户创建身份后进入首次设置引导 |
| UX-P1-03 | Phase 1 | 将导航缩减为三个入口 | ✅ 已完成 | UX-P1-02 | 今日、收藏与追踪、设置 | 导航配置和归属测试通过 |
| UX-P1-04 | Phase 1 | 将历史简报合并到今日页 | ✅ 已完成 | UX-P1-02 | 可查看今日及往期简报 | 保持现有简报 API |
| UX-P1-05 | Phase 1 | 将运行、质量和画像移动到高级入口 | ✅ 已完成 | UX-P1-03 | 一级导航不再暴露内部功能 | 设置页保留三个高级入口 |
| UX-P1-06 | Phase 1 | 生成完成后自动展示最新简报 | ✅ 已完成 | UX-P1-02 | 无需再次进入历史选择 | 成功后刷新并打开最新简报 |
| UX-P1-07 | Phase 1 | 补齐今日页自动化测试 | ✅ 已完成 | UX-P1-01～06 | 核心状态和导航回归通过 | 全仓库 17 个测试文件、66 项测试通过 |
| UX-P1-08 | Phase 1 | 启动项目并验证本地可访问 | ✅ 已完成 | UX-P1-07 | API 健康检查和 Web 首页返回 200 | 开发服务器已提供当前工作区的今日页代码 |
| UX-P2-01 | Phase 2 | 设计三步首次使用引导 | ✅ 已完成 | Phase 1 | 步骤、字段和退出策略明确 | 内容、时间、接收方式三步；高级项不阻塞 |
| UX-P2-02 | Phase 2 | 实现首次配置流程 | ✅ 已完成 | UX-P2-01 | 保存并生成第一份简报 | 复用现有用户、订阅和运行 API |
| UX-P2-03 | Phase 2 | 重组设置页面 | ✅ 已完成 | Phase 1 | 内容、时间、接收方式分区 | 来源、排除词和 Webhook 已折叠 |
| UX-P2-04 | Phase 2 | 评估分区自动保存 | ✅ 已完成 | UX-P2-03 | 输出决策及失败恢复方案 | 保留显式保存，避免改变持久化与失败恢复语义 |
| UX-P2-05 | Phase 2 | 完成首次使用自动化回归 | ✅ 已完成 | UX-P2-02～04 | 主流程状态和中途退出场景通过 | 18 个测试文件、68 项测试通过；不替代真实用户验证 |
| UX-P2-06 | Phase 2 | 启动项目并验证本地可访问 | ✅ 已完成 | UX-P2-05 | API 与 Web 可访问 | 两端返回 200，未触发真实外部投递 |
| UX-P3-01 | Phase 3 | 精简默认反馈操作 | ✅ 已完成 | Phase 1 | 默认最多三个操作 | 有帮助、不喜欢、收藏；复用现有反馈类型 |
| UX-P3-02 | Phase 3 | 实现负反馈二级原因 | ✅ 已完成 | UX-P3-01 | 原因选择清晰且可撤销 | 三个原因按需展开，就地显示处理结果 |
| UX-P3-03 | Phase 3 | 调整收藏和追踪关系 | ✅ 已完成 | UX-P3-01 | 追踪成为次级操作 | 收藏后才显示继续追踪，不改变数据模型 |
| UX-P3-04 | Phase 3 | 优化简报信息视觉层级 | ✅ 已完成 | UX-P3-01 | 先内容、后依据、再操作 | 来源折叠但保持可访问，标签移到正文依据之后 |
| UX-P3-05 | Phase 3 | 完成阅读与反馈自动化回归 | ✅ 已完成 | UX-P3-01～04 | 主操作状态和 API 调用回归通过 | 19 个测试文件、71 项测试通过；不替代真实体验验证 |
| UX-P3-06 | Phase 3 | 启动项目并验证本地可访问 | ✅ 已完成 | UX-P3-05 | API 与 Web 可访问 | 两端返回 200，未触发真实外部投递 |
| UX-P4-01 | Phase 4 | 将正常投递改为结果展示 | ✅ 已完成 | Phase 1 | 正常状态不突出手动投递 | 站内、等待、发送中、成功均结果优先，详情折叠 |
| UX-P4-02 | Phase 4 | 优化投递失败恢复 | ✅ 已完成 | UX-P4-01 | 原因、影响和重试就地展示 | 仅失败突出重新发送，并禁用防重复 |
| UX-P4-03 | Phase 4 | 统一生成与投递错误文案 | ✅ 已完成 | UX-P4-02 | 用户能区分失败阶段 | 技术错误映射为用户文案，不暴露原错误码 |
| UX-P4-04 | Phase 4 | 增加投递状态组合回归测试 | ✅ 已完成 | UX-P4-01～03 | web/email/webhook 主状态覆盖 | 纯状态测试，不调用真实外部服务 |
| UX-P4-05 | Phase 4 | 启动项目并验证本地可访问 | ✅ 已完成 | UX-P4-04 | API 与 Web 可访问 | 两端返回 200，未触发真实外部投递 |
| UX-P5-01 | Phase 5 | 执行完整自动化任务回归 | ✅ 已完成 | Phase 1～4 | 首次、日常和失败流程通过 | 新增 4 个跨阶段状态场景并记录自动化边界 |
| UX-P5-02 | Phase 5 | 验证历史兼容和高级入口 | ✅ 已完成 | Phase 1～4 | R1/R2 数据仍可读取 | 历史数据只读检查和 10 个相关 API 均通过 |
| UX-P5-03 | Phase 5 | 运行全量工程检查 | ✅ 已完成 | UX-P5-01～02 | check、build、diff check 通过 | 21 个测试文件、78 项测试通过 |
| UX-P5-04 | Phase 5 | 启动项目并验证本地可访问 | ✅ 已完成 | UX-P5-03 | API 与 Web 可访问 | 两端返回 200，未触发真实外部投递 |
| UX-P5-05 | Phase 5 | 更新 README 与体验文档 | ✅ 已完成 | UX-P5-04 | 使用说明与最终界面一致 | README、体验报告和 Bug 记录已收口 |

### 10.4 UX Phase 1 验证记录

- `npm run check`：通过，17 个测试文件、66 项测试全部通过。
- `npm run build`：通过，API 与 Web 生产构建成功。
- 新增今日页状态测试，覆盖生成中、今日已就绪、最近简报、跳过、空状态和失败状态。
- 新增一级导航测试，固定“今日 / 收藏与追踪 / 设置”三个用户入口，并验证高级功能归属于设置区域。
- 本地运行检查：API `http://127.0.0.1:3000/health` 返回 200，Web `http://127.0.0.1:5173/` 返回 200；开发服务器返回的当前 `App.tsx` 包含今日页和三入口导航代码。

### 10.5 UX Phase 2 验证记录

- 新用户创建身份后进入三步首次设置：关注内容、时间安排、接收方式；最后一次操作保存订阅并生成第一份简报。
- 设置页按内容偏好、时间安排和接收方式分区；排除关键词、来源限制和 Webhook 收入折叠的高级设置。
- 草稿修改继续与已保存订阅分离；离开页面会触发浏览器未保存提醒，设置内也显示未保存和保存结果。
- 自动保存评估结论：本阶段不采用。继续显式保存可避免改变持久化时机、失败恢复和请求并发语义，不需要 API 合同调整。
- `npm run check`：通过，18 个测试文件、68 项测试全部通过；其中首次设置步骤与边界导航测试 2 项，未保存订阅状态测试覆盖中途退出所依赖的脏状态判断。
- `npm run build`：通过，API 与 Web 生产构建成功。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 工作区提示，无空白错误。
- 本地运行检查：API `http://127.0.0.1:3000/health` 返回 200，Web `http://127.0.0.1:5173/` 返回 200；开发服务器确认提供包含首次设置和设置分区的当前源码。
- 本阶段未执行桌面或窄屏人工走查；按当前开发约定，以自动化检查、构建和本地启动作为阶段验证，不将其表述为真实用户验收。

### 10.6 UX Phase 3 验证记录

- 每条新闻默认操作精简为“有帮助 / 不喜欢 / 收藏”三个；不喜欢的具体原因在用户主动展开后显示，并支持再次点击撤回。
- “继续追踪”从默认操作移到收藏后的次级提示，不改变现有收藏或追踪 API 与数据模型。
- 新闻信息顺序调整为标题与摘要、重要性、推荐依据与标签、折叠来源、用户操作，来源链接仍然可访问。
- 条目反馈、收藏、加入追踪和整期评价均增加操作位置附近的处理中、成功、失败、禁用和防重复状态。
- 主要反馈按钮和二级原因按钮设置最小 44px 高度；负反馈展开按钮提供 `aria-expanded` 和 `aria-controls`，继续使用原生按钮保证键盘可操作性。
- 新增渐进反馈纯状态测试，固定三个默认操作、三个负反馈原因和负反馈激活判断。
- `npm run check`：通过，19 个测试文件、71 项测试全部通过。
- `npm run build`：通过，API 与 Web 生产构建成功。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 工作区提示，无空白错误。
- 本地运行检查：API `http://127.0.0.1:3000/health` 返回 200，Web `http://127.0.0.1:5173/` 返回 200；开发服务器确认提供包含渐进反馈和收藏后追踪的当前源码。
- 本阶段未执行桌面或窄屏人工走查；按当前开发约定，以自动化检查、构建和本地启动作为阶段验证，不将其表述为真实用户验收。

### 10.7 UX Phase 4 验证记录

- 投递区域改为结果卡片，覆盖尚未设置、仅站内、等待发送、发送中、已发送、失败和取消七种用户状态。
- 正常状态不再突出“立即投递”；仅等待或取消状态提供次级“现在发送”，仅失败状态突出“重新发送”。
- 失败卡片同时说明外部发送失败和站内简报仍可阅读，重试的处理中、成功、失败和禁用状态显示在原操作位置。
- 技术错误码映射为配置、超时、限流、网络或通用服务错误文案，界面不直接输出服务端原始错误。
- 成功状态只提供折叠的发送详情，不提供误导性的“再次发送”：现有投递幂等键会返回原成功任务，真正强制重发需要修改 API 或投递状态机，必须另行评审。
- 新增投递展示纯状态测试，覆盖 web、email、webhook 及 pending、sending、succeeded、failed、cancelled 组合，并验证错误脱敏。
- `npm run check`：通过，20 个测试文件、74 项测试全部通过。
- `npm run build`：通过，API 与 Web 生产构建成功。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 工作区提示，无空白错误。
- 本地运行检查：API `http://127.0.0.1:3000/health` 返回 200，Web `http://127.0.0.1:5173/` 返回 200；开发服务器确认提供包含结果优先投递卡片的当前源码。
- 本阶段未执行桌面或窄屏人工走查；按当前开发约定，以自动化检查、构建和本地启动作为阶段验证，不将其表述为真实用户验收。

### 10.8 UX Phase 5 验证记录

- 新增 UX R1 跨阶段任务测试，分别覆盖三步首次设置、今日优先与三入口导航、渐进反馈、外部投递失败后保留站内阅读四个场景。
- 本地 SQLite 只读检查发现 3 个用户、8 份简报、31 个简报条目，其中 7 个旧条目缺少至少一个 R2 扩展字段；未读取或输出用户资料、邮箱、密钥或新闻正文。
- 使用已有历史数据验证用户、订阅、历史列表、历史简报详情、反馈、收藏、追踪、个性化画像、质量指标和版本对比共 10 个只读 API，全部返回 200。
- 开发服务器当前源码检查确认今日页、三步首次设置、渐进反馈、投递恢复以及个性化画像、运行详情、质量概览入口仍然存在。
- `npm run check`：通过，21 个测试文件、78 项测试全部通过。
- `npm run build`：通过，API 与 Web 生产构建成功。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 工作区提示，无空白错误。
- 本地运行检查：API `http://127.0.0.1:3000/health` 和 Web `http://127.0.0.1:5173/` 均返回 200，项目保持运行，未调用真实邮件或 Webhook。
- README 已更新为今日优先、三步首次设置、渐进反馈、显式保存和结果优先投递的当前使用流程；Bug 记录已追加本轮复杂度问题、原因、修复和验证边界。
- 自动化边界：当前测试以 API 集成与纯状态模型为主，没有浏览器组件交互测试，也没有真实用户可用性研究；画像调权及追踪暂停/恢复等低频高级操作仍主要依赖全局提示。
- 本阶段未执行桌面或窄屏人工走查；按当前开发约定，以自动化检查、构建和本地启动作为阶段验证，不将其表述为真实用户验收。

## 11. 后续建议

UX R1 的 Phase 0～5 已完成。下一轮不建议继续仅凭启发式判断扩展功能，优先收集真实使用反馈：

1. 观察用户能否不经解释完成首次设置并找到今天的简报；
2. 记录“今日 / 收藏与追踪 / 设置”三个入口的真实使用频率；
3. 验证负反馈二级原因和收藏后追踪是否容易理解；
4. 补齐画像调权、追踪暂停/恢复等低频高级操作的就地异步反馈；
5. 如需成功后强制再次投递，先设计新的 API 语义和幂等边界，再进入实施。

## 12. 主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 只隐藏功能，没有改善核心流程 | 用户仍然需要多次跳转 | 先实现完整今日状态，而不是只改导航 |
| 高级功能入口过深 | 诊断和运营能力难以找到 | 在设置中保留明确的高级功能分组 |
| 大型 `App.tsx` 继续膨胀 | 状态混用和回归风险增加 | 按视图逐步拆分，不做一次性重构 |
| 自动保存引入新竞态 | 草稿、请求和服务端状态再次混淆 | Phase 2 单独评审，先保留显式保存 |
| 精简反馈降低数据量 | 个性化信号变少 | 保留二级原因，观察真实使用后再判断 |
| 视觉变简单但错误恢复变弱 | 异常时用户无从处理 | 失败状态必须保留就地原因和下一步 |
| 自动化测试掩盖体验问题 | 工程通过但仍然难用 | 明确记录自动化边界，继续依据用户实际反馈迭代 |

## 13. 决策记录

### UX-ADR-001：日常阅读成为默认入口

决定：已有用户恢复身份后默认进入“今日”。  
原因：简报阅读是最高频、最高价值任务，设置不应成为日常入口。

### UX-ADR-002：一级导航缩减为三个

决定：一级导航仅保留“今日”“收藏与追踪”“设置”。  
原因：历史、运行、质量和画像属于上下文或高级能力，不需要长期争夺一级注意力。

### UX-ADR-003：第一阶段不引入自动保存

决定：先保留明确保存，并继续区分草稿与已保存状态。  
原因：自动保存会引入请求频率、失败恢复和并发更新语义，需要单独设计与验证。

### UX-ADR-004：正常投递不作为主操作

决定：默认展示投递结果，仅在失败时突出重试。  
原因：主动推送应由系统自动完成，用户不应日常管理投递任务。
